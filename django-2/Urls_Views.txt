Urls - They are responsible to reach the website to access the particular page 

Have different urls for different pages, different response is being send to the users website

Each urls is maped to certain action 

Views - actions that trigged from the urls, Have main or core logic http gets executed.
It is a fuction in django 

They works to together 

=================================================================================

#Dynamic path segements
We have to keep on adding urls here for every view we create in views.py of blogs app

Used to capture the blog parameter 
Same identifier as used in urls.py should be used in the view function parameter
Blogpost views will be called for every blog there , In future to support more blogs by adding more elif conditions.

===============================================================================

#Path converters
this basically converts the value entered in the url which comes to the path and passed as an argument

Excepting strings
<str:blog> This tells django that value entered here should be treated or converted as a strings

    path("allposts/<int:blog>", views.blog_post_by_number),   #It tells django to convert the given value to integer.

It helps to write different logics of are url patterns depending on the dynamic segments

====================================================================================

#Slugs- A built-in path converter
This can be used as a url segement 
The url which contains alphabets,numbers,hypens are called Slugs no other special characters
checks the url value has slugs format or not

Getting html response adding h1 tags to all response and return fuction variable

==========================================================================================

Making code more dynamic
# Making the code more dynamically using dictionary, We can keep on adding more blocks.
blog_names = {
    "python-intro": "<h1>Python Post</h1>",
    "django-basics": "<h1>Django basics blog posts</h1>",
    "python-oops": "<h1>Object Oriented Programming with python</h1>",
    "regx": "<h1>Regular Expressions in Python</h1>"
}

 
Use
    try:        #To avoiding the error if blog not found in dictionary
        res = blog_names[blog]
    except:    
       return HttpResponseNotFound("<h1>Blog not found</h1>")
    else:
       return HttpResponseNotFound(res)